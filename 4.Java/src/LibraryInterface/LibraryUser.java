package LibraryInterface;

public interface LibraryUser {

   //methods, interfaces are set public as default so modifier would be redundant if added
   void registerAccount();
   //void is a keyword used at method declaration to specify that method does not return any type
   void requestBook();

}
